import informationIcon from "../../assets/Information-Icon.svg";

export default function InformationIcon({ className, style }) {
    return <img style={style} className={className} src={informationIcon} alt="" />;
}