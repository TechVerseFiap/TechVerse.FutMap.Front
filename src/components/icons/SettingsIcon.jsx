import settingsIcon from "../../assets/Settings-Icon.svg";

export default function SettingsIcon({ className, style }) {
    return <img style={style} className={className} src={settingsIcon} alt="" />;
}